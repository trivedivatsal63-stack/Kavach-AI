fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
fastembed>=0.5,<1.0
# CPU ORT by default: coexists with TensorRT-LLM on the same GPU host.
# runpod-setup.sh force-reinstalls this after fastembed's transitive deps.
onnxruntime>=1.21,<1.27
# VRAM probing for the GPU->CPU fallback decision.
nvidia-ml-py>=12.0,<13.0
