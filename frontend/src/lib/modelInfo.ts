/** Served model id — must match TRT_SERVED_NAME / LiteLLM model_name / CHAT_MODEL. */
export const MODEL_NAME = "llama-3.1-8b-instruct";

/** Short human-readable config shown in UI chrome. */
export const MODEL_LABEL =
  "Llama 3.1 8B Instruct · FP16 · 16K context";
